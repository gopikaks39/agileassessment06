# Emergency Ambulance Dispatch System

Friend submission version. Java 17 + Maven. Includes priority dispatch, ambulance compatibility, lifecycle validation, automatic waiting-queue allocation, history, custom exceptions, and positive/negative JUnit tests.
